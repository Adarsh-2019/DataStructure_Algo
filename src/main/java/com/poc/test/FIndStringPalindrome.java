package com.poc.test;

import java.util.stream.IntStream;

public class FIndStringPalindrome {
    public static void main(String[] args) {
        String str="madam";
        /*//boolean isPalindrome = IntStream.rangeClosed(1, str.length() / 2).noneMatch(i -> str.charAt(1) != str.charAt(str.length() - 1 - i));
        boolean isPalindrome = IntStream.rangeClosed(0, str.length() / 2).noneMatch(i -> str.charAt(i) != str.charAt(str.length() - i - 1));
        if (isPalindrome){
            System.out.println("This is Palindrome");
        }else {
            System.out.println("This is no a palindrome");
        }*/
    }
}
